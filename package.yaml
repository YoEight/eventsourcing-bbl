# This YAML file describes your package. Stack will automatically generate a
# Cabal file when you run `stack build`. See the hpack website for help with
# this file: <https://github.com/sol/hpack>.
benchmarks:
  eventsourcing-venteprivee-bbl-benchmarks:
    dependencies:
    - base
    - eventsourcing-venteprivee-bbl
    - criterion
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: benchmark
category: Other
description: eventsourcing-venteprivee-bbl is a new Haskeleton package.
executables:
  eventsourcing-venteprivee-bbl:
    dependencies:
    - base
    - eventsourcing-venteprivee-bbl
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: executable
extra-source-files:
- CHANGELOG.md
- LICENSE.md
- package.yaml
- README.md
- stack.yaml
ghc-options: -Wall
github: YoEight/eventsourcing-venteprivee-bbl
library:
  dependencies:
  - base
  - ansi-terminal
  source-dirs: library
license: MIT
maintainer: Yorick Laupa
name: eventsourcing-venteprivee-bbl
synopsis: A new Haskeleton package.
default-extensions:
  - OverloadedStrings
  - RecordWildCards
  - NoImplicitPrelude
dependencies:
  - classy-prelude
tests:
  eventsourcing-venteprivee-bbl-test-suite:
    dependencies:
    - base
    - eventsourcing-venteprivee-bbl
    - tasty
    - tasty-hspec
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: test-suite
version: '0.0.0'
